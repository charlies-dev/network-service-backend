server:
  port: 8082

spring:
  application:
    name: recruitment-service 
  cloud:
    consul:
      host: localhost
      port: 8500

      discovery:
        hostname: localhost
        enabled: true
        register: true